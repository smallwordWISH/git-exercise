<h1>User List</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Role</th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><a href="<%= user_path(user) %>"><%= user.name %></a></td>
      <td><%= user.role %></td>
    </tr>
  <% end %>
</table>